# 多智能体情绪感知供应链协同决策系统
# Multi-Agent Emotion-Aware Supply Chain Collaborative Decision System
#
# Python >= 3.9
# 核心依赖: 纯NumPy实现DQN, 无需深度学习框架

numpy>=1.21.0,<2.0          # 数值计算 / DQN前向反向传播
PyYAML>=5.4                 # 配置文件加载 (config.yaml)
matplotlib>=3.5             # 可视化 (学术级矢量图表)
networkx>=2.6              # 情绪传染路径可视化
pandas>=1.3                # 实验数据分析与CSV处理
pettingzoo>=1.21           # 多智能体环境框架 (AECEnv)
scipy>=1.7                 # 统计分析 (皮尔逊相关)

# 可选依赖 (持续学习实验)
# torch>=1.12               # EWC的Fisher矩阵计算 (当前用NumPy实现, 非必需)
